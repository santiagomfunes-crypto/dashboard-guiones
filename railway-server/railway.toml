[build]
builder = "DOCKERFILE"
dockerfilePath = "railway-server/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60

overlapSeconds = "30"
drainingSeconds = "15"

restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 0
